<Window x:Class="Phobos.Components.Arcusrix.Desktop.PCOInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Width="400"
        Height="200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">

    <Border x:Name="MainBorder"
            Background="{DynamicResource Background2Brush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="24"
            RenderTransformOrigin="0.5,0.5">

        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform x:Name="DialogScale" ScaleX="1" ScaleY="1"/>
                <TranslateTransform x:Name="DialogTranslate" Y="0"/>
            </TransformGroup>
        </Border.RenderTransform>

        <Border.Effect>
            <DropShadowEffect Color="Black"
                              BlurRadius="20"
                              ShadowDepth="0"
                              Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock x:Name="TitleText"
                       Grid.Row="0"
                       Text="Input"
                       FontSize="{DynamicResource FontSizeLg}"
                       FontWeight="{DynamicResource FontWeightSemibold}"
                       Foreground="{DynamicResource Foreground1Brush}"
                       Margin="0,0,0,16"/>

            <!-- 提示文本 -->
            <TextBlock x:Name="PromptText"
                       Grid.Row="1"
                       Text="Please enter a value:"
                       FontSize="{DynamicResource FontSizeMd}"
                       Foreground="{DynamicResource Foreground2Brush}"
                       Margin="0,0,0,12"/>

            <!-- 输入框 -->
            <TextBox x:Name="InputTextBox"
                     Grid.Row="2"
                     Background="{DynamicResource Background3Brush}"
                     Foreground="{DynamicResource Foreground1Brush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1"
                     Padding="12,8"
                     FontSize="{DynamicResource FontSizeMd}"
                     CaretBrush="{DynamicResource Foreground1Brush}"
                     VerticalAlignment="Top"
                     Height="36"
                     Margin="0,0,0,16">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <!-- 按钮 -->
            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button x:Name="CancelButton" Style="{StaticResource PhobosButtonSecondary}"
                        Content="Cancel"
                        Width="80"
                        Height="32"
                        Background="{DynamicResource Background3Brush}"
                        Foreground="{DynamicResource Foreground1Brush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        FontSize="{DynamicResource FontSizeSm}"
                        Cursor="Hand"
                        Margin="0,0,12,0"
                        Click="CancelButton_Click">
                </Button>

                <Button x:Name="OkButton" Style="{StaticResource PhobosButton}"
                        Content="OK"
                        Width="80"
                        Height="32"
                        Background="{DynamicResource PrimaryBrush}"
                        Foreground="{DynamicResource Foreground1Brush}"
                        BorderThickness="0"
                        FontSize="{DynamicResource FontSizeSm}"
                        Cursor="Hand"
                        Click="OkButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>